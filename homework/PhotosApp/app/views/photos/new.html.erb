<h1>Add A New Photo</h1>
<%= form_for @photo do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %><br>
	<%= f.label :url %>
	<%= f.text_field :url %><br>
	<%= f.submit %>
<% end %>

<%= link_to 'Back', photos_url %>